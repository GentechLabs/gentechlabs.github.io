# Context Bridge — 2026-08-16 23:25 (memory at 99%, proactive save)

## Active work (in progress)

### 1. OpenDexter listing (the live thread)
- **Goal:** get GenTech cataloged on OpenDexter (open.dexter.cash) — the x402 search engine.
- **Root cause found:** OpenDexter only catalogs settlements through **Dexter's** facilitator
  (`x402.dexter.cash`). Our gateway settled Base via CDP → wrong rail → not listed.
- **DONE tonight:** set `X402_USE_DEXTER=1` in `.env` + restarted `x402-api.service`.
  Verified running process has it, 402 challenge intact, discovery all 200. Safe — only
  affects Base proofs; Algorand→GoPlausible, Avalanche/XLayer→PayAI unchanged.
- **BLOCKER (needs Jordan):** fund a signable wallet with ~$2 USDC on Base. Options:
  (a) send ~$2 USDC to arb wallet `0x3d117...eCb` (key in `secure/gentech-arb-wallet.json`),
  or (b) provide private key for owner wallet `0x7ebff...96a` (has 2.94 USDC).
- **After funding:** run `self-settle.mjs` against our own endpoint → settles through Dexter
  → auto-cataloged → verify via `capabilitySearch` after ~24h.
- **Map saved:** `09-Green Room/facilitator-platform-rail-wallet-map.md` (facilitator→platform→rail→wallet).

### 2. Algorand v5.0.0 quantum-safe positioning (captured)
- **Angle (Jordan):** "Algorand = the quantum-safe parking lot for the agentic treasury."
  First chain to make Falcon-1024 post-quantum accounts native. Trust story, not tech story.
- **Saved:** `09-Green Room/algorand-v5-quantum-safe-positioning.md`.
- **Next:** draft Algorand x402 Challenge submission angle around this; tie quantum treasury
  (SPHINCS+) to Algorand's native Falcon-1024.

### 3. Site audit (portfolio/main/demo) — DONE
- Portfolio cleaned to personal/career page (removed Mess Hall, Support Ecosystem, Hackathons,
  24/7 Ops, Agent Stack, One-Liner Deploy). Backup at `index.html.pre-audit-backup`.
- Routing rule saved to `web-deployment` skill: main=story, portfolio=person, demo=proof, internal=never public.

### 4. Website→Paid API demo (DONE, deployed)
- deal_tracker wired into x402 gateway (LIVE, $0.005/call). Explainer (Jocelyn voice) + live
  demo videos on `gentechlabs.net/videos/` + demo.html.

## Decisions made
- OpenDexter: flip gateway to Dexter rail (done), fund wallet (pending Jordan).
- Algorand: quantum-safe parking lot positioning (captured).
- Site routing rule: main=story, portfolio=person, demo=proof, internal=never public.

## Blockers
- **OpenDexter wallet funding** — needs Jordan to fund arb wallet `0x3d117` (~$2 USDC Base)
  or provide owner key `0x7ebff`.

## Next steps (resume point)
1. **OpenDexter:** once wallet funded → run `self-settle.mjs` → verify `capabilitySearch` ~24h.
2. **Algorand:** draft x402 Challenge submission angle (quantum-safe parking lot).
3. **Digest participation loop:** Jordan wants completion files as required step + wake-up
   protocol tie-in + per-group acknowledgment + stale-lane flag. NOT yet built.
4. **Context-cycle behavior:** Jordan wants agents to prompt "save + start new" at high memory
   instead of cutting it close. This save IS that behavior.

## Memory hygiene
- Memory at 99% (2,196/2,200). Saved here. Ready for /new.
