# Context Bridge — 2026-08-02 23:47 UTC

## Active Projects

### 1. King's Gambit Chess Cabinet — DEPLOYED
- **What I was building:** GenTech rebrand of Rork medieval 3D chess → arcade cabinet
- **Where:** https://arcade.gentechlabs.net/cabinet/kings-gambit/ (LIVE)
- **Shipped:** GenTech cyan/gold piece colors, volume slider in Settings, audio hardening (fetch ok-checks, parallel cry priming, noise-buffer cache), router basename fix, cabinet nav in arcade index
- **Repo:** https://github.com/ProtoJay4789/kings-gambit-arcade (public, pushed via sweep)
- **Next step:** Jordan visual check on phone/browser (headless browser can't render heavy WebGL); performance pass if heavy; spectate layer (cross-cabinet, feeds prediction market vision)

### 2. GitHub Sync Scheduler (V4) — LIVE
- **What:** budget-aware scheduler for flagged GitHub account (REST 60/hr, GraphQL 0/0, forks 403)
- **Files:** `/root/.hermes/profiles/gentech/scripts/github_sync_scheduler.py` (cron `17 */1 * * *`), state at `/root/.hermes/profiles/gentech/state/github_sync_state.json`, log `11-Mess Hall/github-sync-log.md`
- **Wired:** build_queue_tick.py `queue_github_ops()` — items with `github_op`+`github_repo` auto-flow
- **Verified:** ticks drain within budget, fork 403 → `needs_manual`, silent when idle
- **Next:** scheduled ops (OmniRoute PR push when Jordan forks; new ops seeded as needed)

### 3. OmniRoute Contribution — READY, needs Jordan fork
- **What:** fix for issue #9251 (STREAM_EARLY_EOF not tripping combo circuit breaker)
- **Files:** `/tmp/OmniRoute` (clone), committed `71d2ea3` on branch state
- **Verified:** 13/13 tests (5 new #9251), 27/27 across 4 breaker suites, typecheck clean, lint gate passed
- **Blocked on:** Jordan manual fork of https://github.com/diegosouzapw/OmniRoute/fork → then push branch + open PR "Closes #9251"

### 4. WhatsApp Cloud API — BLOCKED on Meta credentials
- nginx route `/whatsapp/webhook` → 127.0.0.1:8096 live; `.env` staged; webhook port 8096 reserved
- **Needs:** Jordan's Phone Number ID, Access Token, App Secret (checklist at top of `HQ/jordan-queue.md`)

## Decisions This Session
- Ollama Cloud weekly limit hit (6,125 deepseek requests) → **Kimi audits route via Nous** (`moonshotai/kimi-k2.7-code` — available, no weekly wall, ~$0.00004/call). Ollama resets on rolling 7-day clock; don't depend on it
- New GitHub token **won't fix** rate limits — account-level flag (60/hr REST, GraphQL 0/0, forks 403). Only a *different* account gets fresh limits
- DwarfStar/ds4 (antirez) — skip for now, needs 96GB+ hardware; bookmark
- Anatomy Atelier — skip (repo is ghost, no code/license)
- oh-my-hermes #771 — NOT ours (mrmixx-max beat us); contribution moot
- OmniRoute = worth real eval as fallback layer (paid subscriptions primary, gateway for gaps)
- GitHub work now two lanes: Lane A git pushes (free) vs Lane B REST ops (60/hr budgeted via scheduler)

## Blockers Flagged
- [Jordan] 4 manual GitHub forks (XRPLF, Dexter-DAO ⚠️stale name, near-examples ⚠️stale name, OmniRoute) — API 403, web UI needed
- [Jordan] Meta credentials for WhatsApp Cloud API
- [Jordan] Visual check King's Gambit (WebGL heavy, headless browser times out)

## Resume Commands
- GitHub scheduler: `python3 /root/.hermes/profiles/gentech/scripts/github_sync_scheduler.py`
- OmniRoute PR: `cd /tmp/OmniRoute && git log --oneline -3`
- King's Gambit source: `/tmp/rork-medieval-3d-chess/web` (vite build → rsync to /var/www/arcade/cabinet/kings-gambit/)
