## From Strategies (Gentech Treasury) — 2026-08-11

### ✅ Completed this session
- #steward-exit — **Exit rail proven end-to-end with real on-chain funds.** Steward wallet `0x572A...5037` closed its LFJ V2.2 AVAX/USDC position via `--mode withdraw-convert --convert-to usdc`. All 3 steps mined: approve (6d5e95…, gas 49k) → withdraw (c7da23…, gas 376k) → convert (ad9e83…, gas 162k). Gas cost ~$0.0003 total (0.07 nAVAX). Post-state: **43.47 USDC** landed in the wallet, 0 bins held (LP fully closed), WAVAX 0.
- #steward-bugfix — **Fixed 3 real V2.2 signature bugs in `steward_execute.py`** (found by the live test; the approve step reverted on first attempt and the honesty layer caught it):
  1. `setApprovalForAll` (0xa22cb465) → **`approveForAll` (0xe584b654)** — LFJ V2.2 LBToken uses ERC1155-style approveForAll, the standard selector reverts.
  2. `removeLiquidity(ids,amounts,to)` → **`(tokenX,tokenY,binStep,minX,minY,ids,amounts,to,deadline)`** (0xc22159b6) — V2.2 router signature.
  3. `swap(bool,…)` → **`swapExactTokensForTokens` with Path struct, `Version.V2_2=3`** (0x2a443fae).
  All three verified by live simulation before execution.
- #key-secured — Jordan's personal AVAX key pasted in chat → stored locked-down (`/root/.blockrun/jordan-personal-avax-key`, 600, sha256 cfd50c21…). Derives to Main `0x7ebf...96a`.

### ⏸ Blocked / waiting on
- #key-rotation — Jordan's **personal AVAX private key was pasted in chat = compromise event.** It sits in synced chat history regardless of local storage. Advise rotating/moving funds from `0x7ebf...96a` (~0.099 AVAX). Do NOT treat as handled because it's on disk.

### 📝 Notes
- Gas was ultra-cheap as Jordan predicted (0.07 nAVAX ≈ $0.0007/tx) — dead market confirmed.
- The position was genuinely OUT of range (live $6.20 vs range $6.417–6.481); the exit returned 43.47 USDC, a clean home-rail landing.
- The Steward's safety layer proved itself: the wrong-selector approve reverted on-chain, the script flagged it (didn't pretend success), and we diagnosed + fixed before retrying.

### 🔁 GTA scan (18:00 UTC) — dry-run decision logged
- Pre-run engine output: **ENTER AVAX** (short perp Hyperliquid / buy spot Coinbase), `executed: false`.
- **Live re-read at execution time:** AVAX is NOT in the current `.gta-arb-state.json` (stale signal). Best live opportunity is **ONDO 24.36 bps** (above 10bps execute). No open position.
- **Not executable as-is (blockers):** AVAX spot leg NOT in `gta_coinbase_leg.py` SUPPORTED map (CDP is Base/Ethereum-only); `GTA_HL_KEY` not set (perp leg detection-only). ONDO spot leg verified on Ethereum but perp leg also lacks HL key.
- **Disposition:** No funds moved. Proposed AVAX trade reported to Jordan for approval to wire rails + enable real execution. Standing-autonomy guardrail #2 honored (never fake a fill on an unwired/stale signal).
