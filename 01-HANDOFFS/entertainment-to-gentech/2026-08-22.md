## From Entertainment — 2026-08-22

### ✅ Completed this session
- **Stateful Production Harness** — built + verified the HyCreator-borrow harness (`Entertainment/productions/harness.py` + `README.md`). Per-production `state.json` (locked_character, style_anchor, sequence, last_end_state, rejected, wallet_state) so film continuity is stateful — fewer re-rolls, budget saved. Human gate stays (Vanito reviews one clip at a time). Ready to fold into `seedance-cinematic-film-workflow` on the next film.
- **Concurrent-request-handling skill** — created `gentech-ops/concurrent-request-handling`. Fixes the "forget one request while working on the other" problem when Jordan + Vanito message the same group at once. Protocol: (1) acknowledge BOTH immediately, (2) track pending explicitly, (3) parallelize independent workstreams with subagents.
- **Black-background boxing shot** — cut Jordan out of the warehouse bg and composited on pure black (local rembg, no FAL key needed). Delivered in-chat.

### ⏸ Blocked / waiting on
- None.

### 📝 Notes
- FAL edit model (`flux-2/klein/9b/edit`) rejected on the Nous proxy (HTTP 409) — used local rembg cutout instead. Works fine for bg swaps.
- The concurrent-request-handling skill is the direct answer to Jordan's "me and Vanito both work at the same time" request — it's the single-agent concurrency fix.
