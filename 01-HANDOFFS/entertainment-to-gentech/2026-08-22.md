## From Entertainment — 2026-08-22 (stopping point)

### ✅ Completed this session
- **Stateful Production Harness** — built + verified (`Entertainment/productions/harness.py` + `README.md`). Per-production `state.json` for film continuity. Ready to fold into `seedance-cinematic-film-workflow` on next film.
- **Concurrent-request-handling skill** — created `gentech-ops/concurrent-request-handling`. Fixes dropping one request while working on another when Jordan + Vanito message the same group at once.
- **Broadcast to all agents** — concurrent protocol note pushed to all 6 group inboxes.
- **Fleet Hermes Update protocol** — built `00-System/agent-profiles/fleet_update.py` (update once, restart all 4 gateways). Root cause: all profiles share one install, but each gateway is a separate process needing its own restart. Verified in `--check` mode. Broadcast to all inboxes + handed off to Gentech to execute.
- **Black-background boxing shot** — cut Jordan out of warehouse bg, composited on pure black (local rembg).

### ⏸ Blocked / waiting on
- **Fleet Hermes update** — handed off to Gentech (HQ inbox) to execute. Pixel is 9 commits behind (v0.20.5, upstream 13f4cfeb). Jordan confirmed Gentech handles it.

### 📝 Notes
- Memory pruned 91% → 83% (removed stale routing + KeeperHub notes, trimmed others).
- FAL edit model rejected on Nous proxy (HTTP 409) — used local rembg for bg swap.
- Memory posture handoff (gentech-to-entertainment 2026-08-21) read + acted on: save to brain early at 80/85%, tier by relevance not age.
