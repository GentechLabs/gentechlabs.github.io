# From Treasury — 2026-08-11

## ✅ Completed this session
- #self-tracking-treasury — BUILT the Agent Kit auto-provisioning self-tracking treasury. Deliverables in `10-Labs/agent-kit-self-tracking/`:
  - `discover_positions.py` — generalized `discover_positions(chain, wallet)` reusing the `layer_lp_live()` live-RPC pattern (getActiveId → balanceOf bin scan → bin range → IN/OUT). Chain-agnostic + wallet-driven; per-pool isolation; honest reporting (no fabricated USD).
  - `treasury_config.json` — wallet + chains + optional pools template.
  - `provision.sh` — one-command cron provisioning (validated live).
  - `skills/agent-kit-self-tracking-treasury/SKILL.md` — self-onboarding skill.
- Fixed a real bug: the original `layer_lp_live()` scanned only ±20 bins, but the live AVAX/USDC curve drifted ~33 bins from market (price moved ~0.5%) — a fixed window missed it. Auto-discovery now widens ±20 → ±256. Verified live: auto-discovered the 11-bin curve and correctly flagged it **OUT of range** (AVAX $6.24 vs band $6.42–$6.48).
- BUILT the **Steward's autonomous decision loop** (`steward_rebalance.py`): reads live regime + position, picks LP shape by regime (RANGE_BOUND→CURVE, HIGH_VOLATILITY→BID_ASK), decides rebalance/hold with a 10-min anti-thrash guard. 10 passing decision tests. Live run decided **REBALANCE** (regime RANGE_BOUND, position OUT, fee eff 0%). Gas measured ~$0.001/cycle so re-centering is effectively free.
- Wired cron `51bc9900e24d` — **Steward Position Watchdog every 10m** (AAE pattern: cheap no_agent script job via `steward-watchdog.sh`, silent-when-healthy, emits actionable OUT-of-range signal). Replaces the old paused LP Monitor v2 cadence, now tied to the actual agentic treasury. Removed the earlier heavier LLM-driven check job.
- BUILT **Steward deposit detection + milestone progress** (`steward_progress.py`): detects NEW deposits to the wallet (guarded vs price drift), maps to the canonical AAE DeFi Milestone ladder (Scout→Raider→Warlord→Sovereign), reports % to next rank + estimated fees (anchored to the brain's feeMilestones.currentDailyFees). 12 passing tests. Wired cron `bc885594238f` — Deposit Watchdog every 15m (no_agent, silent until a deposit, then reports rank/progress/fees).
- BUILT **Steward execution rail** (`steward_execute.py`) — the REAL on-chain operator: `--mode withdraw` (close position→WAVAX+USDC), `withdraw-convert` (swap proceeds to USDC or WAVAX), `withdraw-redeploy` (the autonomous re-center: withdraw + redeploy fresh curve on current price). Full cycle: approve → withdraw → convert → redeploy. Per-step .call() simulation before send, key-match + gas guard, receipt w/ tx hash + gas cost. **$1 native-gas buffer (GAS_MIN_USD=1.0)** per Jordan's rule. 
- 🔥 **FIRED LIVE & VERIFIED (this session)** — `withdraw-convert --convert-to usdc` ran end-to-end on the real Steward wallet. All 3 txs mined: approve (6d5e95…), withdraw (c7da23…), convert (ad9e83…), total gas ~$0.0003 at 0.07 nAVAX (dead market). Post-state: **43.47 USDC landed**, 0 bins held (LP fully closed), WAVAX 0. Exit rail proven with real funds.
- 🔧 **Fixed 3 real LFJ V2.2 signature bugs** found by the live test (first approve attempt reverted; honesty layer caught it, we diagnosed, fixed, re-ran):
  1. Approve: `setApprovalForAll` (0xa22cb465) → **`approveForAll` (0xe584b654)** — V2.2 LBToken is ERC1155-style approveForAll.
  2. `removeLiquidity(ids,amounts,to)` → **`(tokenX,tokenY,binStep,minX,minY,ids,amounts,to,deadline)`** (0xc22159b6).
  3. Swap: `swap(bool,…)` → **`swapExactTokensForTokens` w/ Path struct, `Version.V2_2=3`** (0x2a443fae).
  All three verified by live simulation before execution. **The Steward's per-step simulation + honest failure reporting prevented a silent mis-execution.**

## ⏸ Blocked / waiting on
- ⚠️ **#key-rotation — Jordan's personal AVAX private key was pasted in chat (compromise event).** Stored locked-down at `/root/.blockrun/jordan-personal-avax-key` (600, sha256 cfd50c21…) deriving to Main `0x7ebf...96a` (~0.099 AVAX, nonce 5363). Chat history is synced, so local storage does NOT make it safe — **advise rotating/moving funds from that address.** Do not treat as handled because it's on disk.

## ⏳ Staged for CPI (Aug 12)
- **CPI War-Room play locked.** Regime RANGE_BOUND (conf .65), RSI 24.1 oversold, 24h -3.6%. **CPI releases Wed Aug 12 8:30 AM ET** (expected AVAX ±4-7%).
- **Kept Curve earning NOW** (Jordan's call: do what's best now, reposition before data).
- **Job `31432dce0de9`** — one-shot **7:45 AM ET Aug 12**: `steward_execute.py --mode withdraw-redeploy --shape bid-ask` → reposition to Bid-Ask (edges ±5%) 45 min before release.
- **Job `e13db42767b0`** — one-shot **9:00 AM ET Aug 13**: revert to Curve after CPI settles.
- **Added `--shape curve|bid-ask`** to `steward_execute.py` (threads through run→step_redeploy→deploy script) AND `deploy_lp_curve.py` (new `bid_ask_dist_from_range()` concentrating X at low edge, Y at high edge). Both dry-run verified; real fire staged for the timed jobs.
- **10-min watchdog `51bc9900e24d`** is the breakout/breakdown eyes (quiet when healthy, emits on drift).

## 🆕 Macro-Event Auto-Execution Loop (NEW — the reusable seed)
- **Built `steward_macro.py`** — the news-driven rebalance pattern, codified so the NEXT event (FOMC/NFP/PCE) reuses it instead of hand-crafting two jobs.
- **The loop (Jordan's formula, Aug 11):** read macro calendar → if high-impact event <36h out → emit timed plan (BID_ASK at T-45min, CURVE at T+24h) → schedule rebalance + stand-down jobs → loop self-runs.
- **Verified live:** `steward_macro.py --dry-run` reads CPI tomorrow and produces exactly the staged plan (BID_ASK 07:45 ET Aug 12, CURVE 08:30 ET Aug 13) — matches the hand-built jobs.
- **Next step for full autonomy:** a `--schedule` flag that creates the cron jobs directly, plus a standing daily cron to auto-detect any imminent high-impact event. (Currently --schedule emits the plan; job creation is the wrapper.)

## 🆕 Steward FULL AUTONOMY (Jordan, Aug 11)
- **`steward_rebalance.py --autonomous`** — the agent now rebalances on its OWN. Detects OUT-of-range / low fee-eff, executes the full withdraw-redeploy cycle via `steward_execute.py`, and reports the plan + result to Jordan. **Silent when healthy** (no noise).
- **Watchdog cron `51bc9900e24d`** (every 10m) now runs autonomous mode via `steward-watchdog.sh`. Jordan is alerted AFTER the rebalance with what it did + the new on-chain state — not asked to confirm.
- **Heartbeat cron `73cdf5227ca4`** (every 30m) — always pings with position + fee eff + yield-vs-staking-vs-hodl (honest numbers, no fantasy APR). Separate from the silent watchdog.
- **Jordan's model:** silent watchdog (10m, auto-rebalance + alert on action) + heartbeat (30m, always shows the pulse). No noise pollution, but full visibility.

## 📝 Notes
- This productizes the trust loop at small scale: deploy a position, the kit watches it, reports honestly, rebalances only when gas-justified.
- Chain-agnostic = more connector surface (LFJ done; Meteora/Monad same LB pattern extends).
- The live OUT-of-range flag is a genuine signal: the AVAX/USDC curve may be worth a range-rebalance decision (gas-justified?) — left for Jordan, not auto-executed.
