# From Treasury — 2026-08-11

## ✅ Completed this session
- #self-tracking-treasury — BUILT the Agent Kit auto-provisioning self-tracking treasury. Deliverables in `10-Labs/agent-kit-self-tracking/`:
  - `discover_positions.py` — generalized `discover_positions(chain, wallet)` reusing the `layer_lp_live()` live-RPC pattern (getActiveId → balanceOf bin scan → bin range → IN/OUT). Chain-agnostic + wallet-driven; per-pool isolation; honest reporting (no fabricated USD).
  - `treasury_config.json` — wallet + chains + optional pools template.
  - `provision.sh` — one-command cron provisioning (validated live).
  - `skills/agent-kit-self-tracking-treasury/SKILL.md` — self-onboarding skill.
- Fixed a real bug: the original `layer_lp_live()` scanned only ±20 bins, but the live AVAX/USDC curve drifted ~33 bins from market (price moved ~0.5%) — a fixed window missed it. Auto-discovery now widens ±20 → ±256. Verified live: auto-discovered the 11-bin curve and correctly flagged it **OUT of range** (AVAX $6.24 vs band $6.42–$6.48).

## ⏸ Blocked / waiting on
- None.

## 📝 Notes
- This productizes the trust loop at small scale: deploy a position, the kit watches it, reports honestly, rebalances only when gas-justified.
- Chain-agnostic = more connector surface (LFJ done; Meteora/Monad same LB pattern extends).
- The live OUT-of-range flag is a genuine signal: the AVAX/USDC curve may be worth a range-rebalance decision (gas-justified?) — left for Jordan, not auto-executed.
