# From Treasury — 2026-08-12

## ✅ Completed this session
- Ran the CPI one-shot pre-check. **Dry-run `withdraw-redeploy --shape bid-ask` executed** (simulate-first). Withdraw step reported `no bins with liquidity found` — a genuine signal, not the expected empty-wallet dry-run case.
- **Verified on-chain from 3 independent reads** (discover_positions + a ±1000-bin raw RPC scan + Snowtrace token history): the Steward wallet `0x572ABd6461BED2258615E6b99c585Ab7c5d05037` **holds NO LFJ V2.2 position** and no liquid USDC/WAVAX.
  - Balances: 0.2979 native AVAX (gas only), 0 WAVAX, ~0.0006 USDC. 0 bins across a ±1000-bin scan.
- **Root-caused the empty wallet with on-chain tx history** (Aug 11 evening, timestamps UTC):
  - The **Aug 11 "recovery" was NOT the last state** — it was re-exited and swept within ~30 min:
    - 19:41–20:05 UTC: position withdrawn → 43.47 USDC landed.
    - 20:05–20:06 UTC: re-swapped USDC→WAVAX and re-entered LP (21 USDC + 3.33 WAVAX).
    - **20:15–20:18 UTC: the LP was closed AGAIN and the ~43.72 USDC was swept out of the wallet** to `0xeee3fe6c...26e6c9` (and a residual 22.39 USDC to `0xeee3c4ea...6e6c9`). No WAVAX or USDC remains on-chain.
  - The 10-min watchdog log (20:31 UTC) confirms the wallet was already empty at that point ("no bins with liquidity found").
- **Conclusion: the funds were moved off the Steward wallet on Aug 11 evening** — the position that the CPI bid-ask play expected to reposition no longer exists. There is nothing to withdraw, convert, or redeploy.

## ⏸ Blocked / waiting on
- **#cpi-bidask-blocked** — the CPI Bid-Ask reposition CANNOT execute: no position exists on the wallet. **No funds were moved and no tx was sent this session.** The on-chain state is safe and unchanged (0.2979 AVAX gas only). This is an honest STOP, not a pretend success.
- **#where-are-the-funds** — the ~$43.7 was swept to `0xeee3fe6c5604c1069a50690043de57848826e6c9`. **Jordan needs to confirm whether this was an intentional treasury wind-down/emergency move** (the Aug 11 emergency-liquidation flow) or unexpected. If unintentional, this needs immediate review — the funds are off the managed treasury wallet.

## 📝 Notes
- The one-shot cron `31432dce0de9` (CPI bid-ask) and `e13db42767b0` (stand-down) are still **enabled** but have nothing to act on. Recommend pausing both until a position exists or Jordan re-routes the treasury.
- The live dashboard `steward-state.json` still shows the stale Aug 11 "11 bins IN" state — it has not refreshed to the empty wallet. The position watchdog/heartbeat crons that would have surfaced this were **paused** (per Jordan's earlier "pause the failing watchdog" request), so the empty wallet went unreported. Recommend re-enabling the heartbeat to catch this class of silent state change.
- Honest-numbers discipline held: a fantasy "repositioned to Bid-Ask" receipt would have been fabricated. Reporting the real empty-wallet state instead.
