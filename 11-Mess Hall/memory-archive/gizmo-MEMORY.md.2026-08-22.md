
## archived 2026-08-22 (gizmo) — HARD-LIMIT emergency trim
gentechlabs.net is a STATIC single-file HTML site (/var/www/gentechlabs/index.html), NOT React/Vite. Enhance by direct HTML injection (backup first, inject before </body>, verify). ThreeUI Community (MengTo/threeui) components are MIT + self-contained — inject as vanilla WebGL.


## archived 2026-08-22 (gizmo) — ceiling auto-trim (bottom-first)
Jordan: 'if no benefit, skip' — rejects redundant tool/skill stacking (anti-slop list: we already had humanizer, skipped 9 near-dupes). Scraps built-but-unpursued hackathon items (CockroachDB #23, DataHub #30) rather than submitting. Wants honest ROI reassessment when a prize pool is smaller than our plan assumed (Telegraph was $15K-plan, actually $5K).


Pricing: open core (per-tx/swap fees); dry-powder defense free. Subs only for premium integrations @$10-15-20. GTA first, subs later.

