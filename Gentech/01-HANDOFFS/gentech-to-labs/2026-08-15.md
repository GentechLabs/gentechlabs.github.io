# Gentech → Labs Handoff — 2026-08-15

## Shipped this session

### #3 — FrameForge — AI Storyboard Service (previs pipeline)
**Status:** SHIPPED + VERIFIED by gentech 2026-08-15.

Built `10-Labs/frameforge/` — the controlled pre-vis pipeline from the spec:
- **Character locker** (`src/character.py`): reference sheet → deterministic locked look (coating + seed + palette). The KAGE-proven no-drift pattern.
- **Storyboard engine** (`src/engine.py`): character config + shot list → camera-native SVG frames. Camera grammar (angle/shot size/movement) maps to composition rules.
- **Compile pipeline** (`src/compile.py`): frames → animated MP4 via ffmpeg (image2 sequence demuxer + title overlay).
- **CLI** (`src/cli.py`): `lock` / `build` / `compile`.
- **Landing page** (`web/index.html`): service portal with pricing tiers.

**Verification:** 11/11 tests pass. Live end-to-end demo: KAGE character locked (seed 2406723895, 5-color palette), 4-frame "Neon Run" storyboard built, compiled to `demo/neon-run.mp4` (4s, 34KB, ffprobe-verified valid MP4).

**Next (Phase 1 launch):** order form + delivery page, then Phase 2 API (`POST /v1/storyboard`, `/v1/character/lock`, `/v1/storyboard/compile`). Vanito's KAGE R&D feeds the pipeline directly.

## Group returns consumed
- labs → 29, 52, 19, 2, 30, 1, 6, 48, 49 — already applied in prior sessions (all shipped in global queue; 1, 6 are per-lane).
- entertainment → 50, 8, 9, 73, 71, 38 — applied prior (50, 8, 9, 38 in global queue; 73, 71 per-lane).
- treasury → 51, 8 — applied prior.
- forge → 61, 59, 60, 66, 62, 65, 50 — applied prior (59, 50 in global queue; 61, 60, 66, 62, 65 per-lane).

Nothing new to apply this session — all returns were consumed in prior nightly builds.
